import { Injectable } from '@angular/core';
import { Sticker1 } from './sticker.model';
import { Observable, from } from 'rxjs';

@Injectable()
export class StaticDataSource {
  private stickers: Sticker1[] = [

]
}